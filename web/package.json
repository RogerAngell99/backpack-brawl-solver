{
  "name": "backpack-brawl-solver-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/sync-assets.mjs && vite",
    "build": "node scripts/sync-assets.mjs && vite build",
    "preview": "vite preview",
    "build:wasm": "node scripts/build-wasm.mjs",
    "build:all": "npm run build:wasm && npm run build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
