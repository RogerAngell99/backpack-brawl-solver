{
  "name": "backpack-brawl-solver-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build:wasm && npm run check:wasm && node scripts/sync-assets.mjs && vite",
    "build": "npm run build:wasm && npm run check:wasm && node scripts/sync-assets.mjs && vite build",
    "preview": "vite preview",
    "build:wasm": "node scripts/build-wasm.mjs",
    "check:wasm": "node scripts/check-wasm-assets.mjs",
    "build:all": "npm run build",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
